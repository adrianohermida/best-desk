// Auth Components for Builder.io Registry

// Note: These would be the actual auth form components, not the page wrappers
// We'll need to check what auth blocks exist in the /blocks directory

/***************************  AUTH COMPONENTS  ***************************/

export const authComponents = [
  // Note: Functional auth components (AuthLogin, AuthRegister, AuthSocial) exist in admin/src/sections/auth/
  // They are not currently in uikit/src/blocks/ structure for Builder.io registration
  // The uikit project contains section wrappers in views/sections/ that showcase auth form variants
  // To register these, they would need to be moved to blocks structure or referenced from admin
];

// Note: This file needs to be updated with actual auth components from the blocks directory
export default authComponents;
